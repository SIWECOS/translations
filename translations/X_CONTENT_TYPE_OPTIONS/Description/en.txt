The X-Content-Type-Options settings in the header prevent that the [[Browser]] interprets data as anything other than declared by the content type in the  [[HTTP|HTTP-Header]]. The header settings are not set here.